{
  "name": "Python Lab",
  "dockerFile": "Dockerfile",
  "context": "..",
  "postCreateCommand": "pip install -r requirements.txt",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-toolsai.jupyter"
      ]
    }
  }
}

